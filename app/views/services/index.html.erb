<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Services

      <%= link_to new_service_path, class: "btn btn-lg btn-outline-secondary" do %>
        <i class="fas fa-plus fa-fw"></i>
      <% end %>
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Cost
            </th>

            <th>
              Description
            </th>

            <th>
              Notes
            </th>

            <th>
              Part
            </th>

            <th>
              Shop visit
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @services, partial: "table_row", as: :service %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @services.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :service %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Services

        <%= link_to new_service_path, class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fas fa-plus fa-fw"></i>
        <% end %>
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @services, partial: "list_item", as: :service %>
      </div>
    </div>
  </div>
</div>
