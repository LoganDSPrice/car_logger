<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Shops

      <%= link_to new_shop_path, class: "btn btn-lg btn-outline-secondary" do %>
        <i class="fas fa-plus fa-fw"></i>
      <% end %>
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Name
            </th>

            <th>
              Street address
            </th>

            <th>
              City
            </th>

            <th>
              State
            </th>

            <th>
              Zip code
            </th>

            <th>
              Phone number
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @shops, partial: "table_row", as: :shop %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @shops.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :shop %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Shops

        <%= link_to new_shop_path, class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fas fa-plus fa-fw"></i>
        <% end %>
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @shops, partial: "list_item", as: :shop %>
      </div>
    </div>
  </div>
</div>
